<%= form_for @night do |f| %>
  <% if @night.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@night.errors.count, "error") %> prohibited
      this post from being saved:</h2>
    <ul>
    <% @night.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :Date %><br>
    <%= f.text_field :date %>
  </p>

	<h2>Players</h2>
	<%= f.select :player_ids, Player.all.collect {|x| [x.name, x.id]}, {}, :multiple => true %>
		
  <p>
    <%= f.submit %>
  </p>
<% end %>